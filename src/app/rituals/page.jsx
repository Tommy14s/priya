import RitualsPageContent from "@/components/rituals-page-content";

export const metadata = {
  title: "Rituals Menu - Priya Thai Massage",
  description: "Choose a Thai massage ritual and compare durations before booking.",
};

export default function RitualsPage() {
  return <RitualsPageContent />;
}
